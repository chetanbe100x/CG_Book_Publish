# Book-specific adapters

Adapters are added only after an actual DOCX audit identifies a feature that
cannot pass the shared fidelity gates. An adapter must be scoped to one job,
covered by a regression fixture, and must not weaken package or render checks.
